namespace PlatenReports.Pdf;

/// <summary>
/// Placeholder for the PDF layout engine and output.
/// </summary>
/// <remarks>
/// This package is published at 0.0.0 to reserve the package id. NuGet has no
/// scopes, so an unreserved id is available to anyone. There is no
/// implementation here yet — see the repository README for scope and status.
/// </remarks>
public static class PlatenReportsPdfPlaceholder
{
    /// <summary>Always <see langword="false"/> until the renderer ships.</summary>
    public static bool IsImplemented => false;
}
